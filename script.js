document.addEventListener('DOMContentLoaded', () => {
    console.log("Birthday card loaded!");
    // Nanti kita bisa tambahkan logika interaktif di sini, 
    // seperti tombol untuk memainkan lagu atau animasi confetti.
});
